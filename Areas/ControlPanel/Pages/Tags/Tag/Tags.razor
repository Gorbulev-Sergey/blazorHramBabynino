@inject ITagsService TagsService  

<TagsSearcherForCPanel inserted="true" удалить="удалить"></TagsSearcherForCPanel>

@code {
    void удалить(tag tag)
    {
        TagsService.remove(tag);
    }
}
