@page "/cpanel/photos/edit/{ID}"
@attribute [Authorize(Roles = "администратор, редактор")]
@{
    ViewBag.Title = "Редактировать фотоальбом";
}

<component type="typeof(razorHramBabynino.Areas.ControlPanel.Pages.Photos.Photos.Edit)" render-mode="ServerPrerendered" param-ID="ID" />

@functions{ 
    [BindProperty(SupportsGet = true)]
    public int ID { get; set; }
}